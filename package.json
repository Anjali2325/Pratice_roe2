{
  "name": "playwright-sum-tables",
  "version": "1.0.0",
  "description": "Scrape pages, sum numbers in tables using Playwright",
  "main": "sum_tables.js",
  "scripts": {
    "start": "node sum_tables.js"
  },
  "dependencies": {
    "playwright": "^1.34.0"
  },
  "engines": {
    "node": ">=18"
  }
}
